#!/bin/sh

{{ if not .is_termux -}}

if [ -t 0 ] && [ -t 1 ]
then
  shell=$(getent passwd {{ .username }} | awk -F : '{print $NF}')
  if [ $shell != $(which zsh) ]
  then
    chsh -s $(which zsh) {{ .username }}
  fi
fi


{{ end -}}
