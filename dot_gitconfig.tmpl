[user]
	email = {{ .email }}
	name = {{ .name }}
	signingkey = {{ .signingkey }}
{{- if eq .username "technat" }}
# for any remote system other usernames are used (e.g codespace or ssm-user)
[commit]
	gpgsign = true
{{- end }}
[pull]
	rebase = true
